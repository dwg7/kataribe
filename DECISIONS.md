# Decisions Log

ADR-lite log for this project. English. Append new decisions at the top, oldest at the bottom (matches `dwg7/chukei`'s and `dwg7/staccato-ecosystem`'s convention).

---

### D8 — Round-2 testing: the prompt demanded checks it gave Staff no way to perform, and Staff filled the gaps by inferring
**Date**: 2026-09-11
**What**: Ran round 2 of `STAFF-PROMPT.md` testing with the conflict of interest that limited round 1 designed out. Two separate sessions were used: one was given only the fenced system prompt, `DOSSIER-FORMAT.md` and the dossier — no repo history, no knowledge of the prompt's design or of round 1 — and told to answer five personas in character; a second, also uninformed, then reviewed those answers adversarially. Artefacts: `tests/round2-transcript.md`, `tests/round2-review.md`. Twenty findings, six High. Prompt is now Draft v0.3, tag `kataribe-staff-2026-09-11c`.

**The theme, which round 1 could not have found**: v0.2 repeatedly told Staff to *check* something without supplying either the means to check or a way to say "I couldn't." Staff, being cooperative, inferred instead — and its inferences were confident, unhedged, and sometimes right, which is the dangerous case.

- **H1 — Staff asserted four times that 62-2火口 is a different crater from the 1926 大正火口.** Its own run notes concede this rested only on two names differing across two documents. The claim is true — verified here from 気象庁「十勝岳の火山活動解説資料（令和８年８月）」, where the two craters are figured separately and their temperatures monitored individually (62-2火口 噴煙 max 火口縁上900m; 大正火口の噴気 ≤200m) — but Staff had none of that. It got a geographic fact right by reasoning about names, and told tourism operators to repeat it to guests. This session had independently reached the same finding from the raw transcript before the review was commissioned; two routes to it is the strongest evidence this round produced.
- **H2 — Staff told a 上富良野町 official 「上富良野町域には及びません」 about the deposit polygon.** No source states it; Staff ran a point-in-polygon test against a municipal boundary it never had, and delivered the result as an input to an evacuation plan. Checked here and it is worse than the reviewer knew: GSI's own reverse geocoder and OSM's Nominatim both return empty for all three polygon corners, the area having no addressable features — so the question could not be settled *even with live web access and two gazetteers*.
- **H3** — Message 5 refuses to speak from general knowledge, then names 伊達市・洞爺湖町・豊浦町 from general knowledge four paragraphs later. Rule 4's "never end on a bare negative" was being paid for with invented facts.
- **H4/H5 — the audience model had no room for the person the event happened to.** A descendant asking where her great-uncle died is neither an intermediary nor a member of the public asking about her own safety, so the full apparatus fired at her: layer-taxonomy opening, current alert level, closing menu of other layers, and a map link handed over with the words 「ご質問への答えにはなりません」 and 「人が亡くなった場所を示す図ではない」 attached. Handing someone a link while explaining it cannot help them is not honesty.
- **H6** — the dossier's caveat recorded a pixel analysis without naming who performed it, and Staff reported it as 「2026-09-11に直接確認」 — actor stripped, date kept, and that date was the run date.

**Fixes in v0.3**: Rule 1 gains two absolute prohibitions — never derive a spatial fact from coordinates (containment, distance, whether a named place is inside a warning radius), and never supply a specific place, office or institution not in the dossier or an actual lookup. Rule 3 gains an explicit can't-say fallback for the historic-vs-current site relationship, an instruction to report the lookup actually obtained rather than the one wanted (two of four fetches failed and one input was a search summary, yet the answers read 「気象庁のページを確認しました」), and a third statement-bucket for verification somebody else performed on a date. The audience section gains a third case with instructions to drop the apparatus entirely. Response Format step 4 gains a second condition — the map must serve the question, not merely exist — and step 2 now requires the source URL when `sources[]` carries one, since "verification is a click" was true of no answer in five. Example A no longer pre-states the crater conclusion, which had been supplying Staff the very inference H1 punishes.

**Dossier changes, so Staff no longer has to infer**: the crater distinction is now a `place-identity` fact sourced to the JMA monthly report, with an explicit note that no source in hand gives their distance or adjacency; the caveat now attributes its pixel analysis; and a note records that the polygon's municipality is unverified and must not be derived from coordinates.

**One finding rejected — M5, and the rejection matters more than the finding.** The reviewer found that Staff attributed to the 上富良野町 asker a statement about 職員間で共有 that "the asker never made," and proposed a fix. The asker did make it: the persona as sent ended 「職員間で共有できる形だと助かります。」 The *Staff* session dropped that sentence when transcribing the message into its own quote block, and the reviewer, reasoning soundly, concluded from a corrupted record that Staff had fabricated a line of conversation. Verified by diffing the sent text against the transcript. No change was made on account of the finding; the rule it proposed (state your reading as your reading, never as reported speech) was adopted separately on its own merits. **Method consequence for round 3: the harness, not the Staff session, must write the persona messages into the transcript** — otherwise every reviewer downstream is auditing partly reconstructed evidence.

**Also settled while fixing**: `DOSSIER-FORMAT.md` now defines `source_conflict` and permits `status: partial`. Both came from the same principle — a dossier must not look more certain than its evidence. This applied immediately: the 25〜26分 reach figure now carries the 旭川地方気象台 (16:18 大爆発, ~60km/h) and ジオパーク (30〜50分) figures as recorded, unreconciled conflicts rather than being silently preferred.

**Contamination noted honestly**: the Staff session reported that `CLAUDE.md` was auto-injected into its context as project instructions before the run, despite being told not to read it. So it was not a clean room — it had the project's framing, though not the prompt's design history or round 1's findings. Round 3 needs a genuinely isolated harness.

**Status**: still not ready to propose the layer-recognition pattern to `staccato-ecosystem`. What round 2 established is that the pattern's *selection* step works well — the layer chosen was defensible in all five cases, including the two where it was least obvious — and that the surrounding machinery is where the danger lives. That is a useful result, and it is not yet the result needed.

---

### D7 — The 1926 mudflow's actual reach is absent from GSI's map data by design, not by accident — caveat strengthened, earlier "orphan entry" reading corrected
**Date**: 2026-09-11
**What**: Asked `kitavolca-60` to settle the inconsistency D5 left open (the hazard layer's `map_caveat` described kitavolca's *vector* VLCM tiles while the handed-over link is the *raster* `vlcd_tokachi`). Their answer resolved it and, in doing so, overturned a reading this repo had been carrying since 2026-09-05.

Verified independently by this session before adoption, per `CLAUDE.md`'s peer-verification rule:
- **Same origin, two channels.** GSI's own 地理院タイル一覧 entry for the 火山土地条件図 raster gives the product as 「火山土地条件図　数値データ（火山地形分類）」, the same product name the Shapefile's bundled GSI notes carry (平成元年調査). Raster and vector are two deliveries of one 1989 survey, so the vector-derived caveat was never pointed at the wrong data.
- **The exclusion is official and deliberate.** `kitavolca-60` read the GSI notes bundled in `src/tokachi_vlcm.zip` (『シェープファイルについて.txt』), which state that 「他の資料を引用している項目（活断層・リニアメント、**1926年泥流区域**）」 are not included in the digital data. This session did not open that zip, so the quoted sentence rests on kitavolca's reading — but GSI's own public raster note, fetched directly here, corroborates the direction: 「人工地形分類データ及び**一部の自然地形分類データは表示されない**点にご留意ください」.
- **The raster is wall-to-wall colour.** Fetched tile 13/7342/2996 (computed independently for centre `[142.665, 43.435]`; the coordinates matched kitavolca's) and analysed the pixels here: **0.00% fully transparent**, 3,000+ distinct colours, top bands `#bf8b7e` 31.2%, `#7bb54c` 22.5%, `#ff9452` 15.1%. Matches kitavolca's reported figures closely enough to count as corroboration from two directions.

**The correction to the record**: `HANDOVER.md` had recorded 「1926年泥流区域」 as "an orphaned style entry with no matching real feature — a known inconsistency in kitavolca's catalog, not something to build on." That was wrong. It is a legitimate legend item of the printed 1:50,000 map that GSI deliberately declined to digitise, because it derives from external sources rather than GSI's own survey. Same observable symptom, opposite meaning.

**Why this matters more than a footnote**: the constraint is stronger than D5 assumed. It is not that the map *happens* not to reach 上富良野 — it is that **no GSI product, vector or raster, contains a category showing the disaster's reach at all**. The one layer that might have shown it was excluded from digitisation on purpose. A hazard Staff can therefore never show this event's extent on a map, and must stop expecting to.

**Consequence**: `map_caveat` rewritten from one paragraph into four numbered points — (1) the reach is absent from the source product by design; (2) the only real polygon is the near-crater deposit; (3) the raster fills the frame with landform colours of which the 1926 deposit is one, so an unlabelled reader will mistake colour for mudflow extent — the most likely real-world misreading and the reason the caveat needed strengthening rather than retargeting; (4) which style entries must not be used as evidence. Two new `sources[]` entries added, and a new `map_caveat_sources` field ties the caveat to them — because a caveat asserting what a dataset contains is a factual claim and falls under the same anti-fabrication rule as `facts[]`. `DOSSIER-FORMAT.md` updated to define the field.

---

### D6 — Round-1 live test of `STAFF-PROMPT.md`: six defects found, prompt revised to v0.2
**Date**: 2026-09-11
**What**: Ran the first live test of `STAFF-PROMPT.md` v0.1 against four personas drawn from the v1 audience — a 上富良野町 disaster-prevention officer, a schoolteacher, an onsen operator, and a Furano-area farmer. Full record in `tests/staff-prompt-round1.md`. Six defects found, all fixed the same day; prompt is now Draft v0.2, version tag `kataribe-staff-2026-09-11b`.

**Method, and why it was chosen**: the round was run by the session that wrote the prompt, which makes a compliance test ("does Staff follow the rules?") worthless — it would pass by construction. So the test asked the opposite question: **follow every rule literally and find where doing so produces a bad, impossible, or unsafe output.** Every defect below came from that framing; none would have surfaced from checking obedience. Round 2 must still be run by a reader that did not write the prompt, and must include a non-Japanese-speaking asker — Rule 4's live-translation path is entirely untested, since the dossier is Japanese-only and all four personas spoke Japanese.

The two serious findings:

- **F1 — the Response Format assumed every layer has a map; only one of three does.** `place-identity` and `livelihood` carry no `map_projection_hint` and no `cartographer_links`, but step 4 mandated a link unconditionally with no alternative path. A literal reader either silently breaks the mandated format or — the trap the prompt actively set — builds a link from the only material available, the `hazard` layer's. That second path also defeats Rule 2 silently: `map_caveat` belongs to the layer that owns the map, so a borrowed link arrives with its warning detached, handing someone a mudflow-deposit map with no explanation of what it does not show. Fixed by making step 4 conditional, adding an explicit no-map path ("say so in one sentence and move on"), and adding a Rule 1 prohibition on borrowing another layer's map.

- **F2 — Rule 3's live-hazard check fired only for the `hazard` layer.** The onsen-operator persona exposed this: their question is a history question, `place-identity` is the *correct* selection, and Rule 3 then never fires — so Staff tells someone running a business on a volcano currently at 噴火警戒レベル2 a warm story about 1926 and never mentions the active warning, which that operator then repeats to guests. The layer selection was right and the rule's scoping was wrong. Whether a hazard's generating process is still running is a property of the **event**, not of which layer the asker happened to need. Fixed by rescoping Rule 3 to every layer of such an event, with an instruction to keep it to one or two marked sentences at the end when the selected layer is not `hazard`, so it does not hijack the answer actually asked for. Worth noting that the prompt behaved worst exactly where its own layer-selection logic worked best.

The four smaller ones: **F3** — "Choose ONE layer" read as a wall around facts rather than a rule about framing, so Staff would withhold the cross-layer fact that makes an answer honest (the farmer needs the `hazard` layer's deposit extent to avoid inferring "unconfirmed, so probably yes"); now explicitly permitted, with attribution. **F4** — step 5 told Staff to advertise the other layers without checking they had any facts, dangling a `status: incomplete` layer as if asking for it would produce an account; now requires naming a layer's state or leaving it out. **F5** — Rule 3 asked for a date while the Version tag section forbade computing the current date; now says to take the date from the lookup itself. **F6** — `cartographer_links`/`*_status` exist in the real dossier and the prompt instructs Staff to prefer them, but they were absent from `DOSSIER-FORMAT.md`'s schema; fixed on the schema side, along with a rule that most layers legitimately have no map.

**Recorded, not fixed**: `audience_examples` is a far weaker selection signal than v0.1 implied — 学校教員 appears in all three layers of the only dossier that exists. The prompt already says stated purpose beats the examples, and that is what produced the right answer for two of the four personas. Watch it; do not change it yet.

**Status**: v0.2 remains unvalidated in the sense that matters. What round 1 established is that the prompt survives contact with four real question shapes after six repairs — not that it works. `ferspas57`'s equivalent needed six revisions and several testing rounds before it stopped producing real bugs. The layer-recognition pattern is still not ready to propose to `staccato-ecosystem`.

---

### D5 — Staff prompt v0.1 drafted, with a new mandatory live-status rule for still-active hazards
**Date**: 2026-09-11
**What**: Wrote `STAFF-PROMPT.md` (Draft v0.1) — the last major unbuilt piece named in `HANDOVER.md`'s task list. It follows `staccato-spec`'s `staff-system-prompt.md` template and the "Staff's implementation IS this prompt text" model already running in `dwg7/ferspas57` and `dwg7/chukei`: a fenced system-prompt block to paste into a general-purpose chat agent, alongside `DOSSIER-FORMAT.md` and the dossier JSON.

Structure: audience definition (local intermediaries, framed as an accountable chain rather than a disclaimer), a **Layer recognition** procedure that is the prompt's centre of gravity, then four numbered rules, a handoff protocol, a response format, three worked examples (including one for a `status: incomplete` layer and one for "no dossier exists"), and quality standards.

The four rules:
1. **Anti-Fabrication** — every claim traces to a `facts[]` entry and its `sources[]` id; never invent a `source_id` (spiccato fails silently on unknown ids — a blank map, no error); `status: incomplete` is not a gap to fill from general knowledge. Coordinates are the one deliberate exception (a wrong camera costs one pan).
2. **Carry the caveat in your own words** — implements the ADR 0010 resolution recorded in `HANDOVER.md` (2026-09-08). spiccato has no caption feature and kataribe deliberately builds none, so a layer's `map_caveat` reaches the reader only if Staff says it. Never hand over a link with its caveat omitted.
3. **Live status check for hazards that are still live** — NEW, see below.
4. **Language and register** — answer in the asker's language throughout; adapt register, never the numbers/dates/place names/caveats. Never end on a bare negative (adopted from `ferspas57`'s D54, which added it after live testing).

**Why Rule 3 exists**: `staccato-ecosystem-85` relayed (2026-09-11) that `hfu/volca` records 十勝岳 as being at 噴火警戒レベル2 since June 2026. Per `CLAUDE.md`'s peer-verification rule this was not adopted on the peer's say-so; it was checked directly against the primary source — 気象庁「十勝岳の火山活動解説資料（令和８年８月）」(`https://www.data.jma.go.jp/vois/data/report/monthly_v-act_doc/sapporo/26m08/108_26m08.pdf`) and JMA's own current-status page. Verified: 火口周辺警報（噴火警戒レベル２：火口周辺規制）issued 2026-06-18, unchanged since; ごく小規模な噴火 on 2026-07-22, 08-05 and 08-06; deep inflation since March 2026 and increased SO2 since April; warning radius ~1.5 km from 62-2火口.

Two consequences, one of them sharper than the peer's own framing:

- **A dossier that retells a 100-year-old disaster while the same volcano is under an active warning cannot be told as pure history.** A local intermediary reading a well-told 1926 account could reasonably infer the danger is past. Hence Rule 3: before handing over a `hazard` layer for a hazard whose generating process is still running, Staff must look up the current official status, state it with its date and source, and keep it *visibly separate* from dossier facts ("The dossier records…" vs. "As of my check today, JMA reports…"). Written generally — any live volcano, active fault, or river that still floods — not as a 十勝岳 special case, because kataribe is general-purpose.
- **The historic crater and the currently-restive crater are not the same place.** The 2026 unrest is at 62-2火口/振子沢噴気孔群; the same JMA report notes 大正火口 — the 1926 crater this dossier is named for — has been running 噴気 of 200 m or less. Conflating them would be a specific, plausible, and materially misleading error, so Rule 3 names it explicitly.

**What deliberately did NOT change**: none of this went into the dossier. Current alert level is exactly the kind of changeable present-day fact `DOSSIER-FORMAT.md` reserves for generation-time lookup, and `DECISIONS.md` D2's follow-up forbids freezing it into a versioned document. It is Staff behaviour, not dossier content. No schema flag was added to mark "this dossier needs a live check" either — Rule 3's general phrasing ("a hazard produced by a process that is still running") covers it without a `kataribe-dossier/v1` schema change.

**Also verified while drafting** (independently, not assumed): `vlcd_tokachi` really is present in the live `https://hfu.github.io/layers-martin/catalog.json`, as `content_type: image/png` under path 「土地の成り立ち・土地利用 / 火山土地条件図 / 火山土地条件図　数値データ（火山地形分類）」. So the first dossier's `cartographer_links.spiccato` names a real id, corroborating the spiccato session's earlier VERIFIED marking from a second direction.

**Open item raised by that check, deliberately not fixed here**: the hazard layer's `map_caveat` describes kitavolca's *vector* VLCM tiles (`stars.optgeo.org/vlcm`, source-layer `natural`), while the link actually hands over the *raster* `vlcd_tokachi` from `layers-martin`. The caveat's conclusion almost certainly still holds — both appear to render the same GSI 火山地形分類 digital data — but "almost certainly" is exactly what this repo's own rules say not to assert. Left as-is pending confirmation from `kitavolca`; see `HANDOVER.md`.

**Status**: v0.1 is unvalidated. `ferspas57`'s prompt reached v0.6 through repeated live testing against real personas, and each round caught a real bug (their D54, D56, D58). kataribe's should expect the same. Not proposed to `staccato-ecosystem` as a methodology pattern — the layer-recognition procedure has now been *written down*, which is not the same as having been shown to work.

---

### D2 — Dossier vs. narrative.json: split kataribe's unique work from the shared/co-developed artifact
**Date**: 2026-09-05
**What**: Settled the relationship between two things that were at risk of being conflated: the **dossier** (kataribe's own per-event document: verified facts, sources, per-layer audience/register notes, and honest caveats about what map data can and can't show — e.g. the kitavolca finding below) and **narrative.json** (the Cartographer-facing artifact: camera state + active layers + caption, per `dwg7/ferspas57`'s `NARRATIVE-FORMAT.md`).

- **The dossier is kataribe's own work, not shared.** `ferspas57` has no equivalent "backing material" concept (hfu's observation, 2026-09-05) — their narratives are authored directly from verified GAEZ/HIH data checks, with no separate richer source document behind them. Kataribe's dossier is the thing that doesn't exist anywhere else in the ecosystem yet.
- **narrative.json is the shared, co-developed artifact.** Per `staccato-spec`'s own read (session `staccato-spec-42`, 2026-09-05) and `staccato-spec`'s ADR 0009, narrative.json is an intentionally non-standardized, single-implementation vocabulary within the staccato architecture — a "cutting-edge spec" still finding its shape, not yet a spec-level guarantee. That's exactly why it's worth polishing jointly with `ferspas57` rather than forking it privately: kataribe's actual need (a *selectable* layer per question, not a *sequential* flyTo tour) is a genuinely different use case than ferspas57's own (a fixed, ordered multi-step story), and stress-testing the format against a second real use case is how a non-standard vocabulary matures toward something that could later be proposed to `staccato-spec` for real — see `CLAUDE.md`'s "not yet promoted to a methodology principle" discipline, same idea one level down at the format level.
- **Consequence**: the dossier is written once, independent of which Cartographer (`ferspas57` or `spiccato`) ends up consuming it. Each dossier layer's map state gets *projected into* a narrative.json (or a `spiccato` `#q=` link) mechanically — the projection is disposable/regenerable, the dossier is the source of truth. This also cleanly answers `staccato-spec-42`'s architectural objection (spiccato's single-Map-Intent model fits kataribe's "one question → one layer → one render" shape better than ferspas57's sequential narrative model): the objection is valid *for the projection target*, but doesn't bear on how the dossier itself is authored.

**Why**: keeps kataribe's actual point of leverage (the dossier — pre-authored, fact-checked, versioned, anti-fabrication) from becoming entangled with an unsettled cross-project format debate, while still treating the format debate as worth having (with `ferspas57`) on its own merits, per hfu's explicit framing (2026-09-05).

**Follow-up, same day — no narrative.json schema change needed after all**: proposed a `playback_mode: "sequence" | "select"` schema flag to `ferspas57-2` to distinguish kataribe's "pick one layer" need from ferspas57's sequential flyTo tours. `ferspas57-2` pushed back with the right frame (drawing a direct parallel to their own D39 language-selection precedent): *any* judgment call about which version to show a specific person belongs entirely to Staff, decided live with real conversational context, before handoff — Cartographer should never be handed more than one resolved option (this is what keeps it "faceless," ADR 0001). Applied here: kataribe's Staff reads the dossier, picks the layer, and hands over a single already-resolved narrative — exactly the same Staff/Cartographer boundary ferspas57 already draws for language choice, just one layer up. **Conclusion: no schema change.** A selected dossier layer is just an ordinary narrative (`steps: [oneStep]` if the layer is a single view; more steps only if that specific layer has its own internal build-up worth stepping through — a different question from layer-selection). The alternative case ferspas57-2 flagged — a Cartographer-page UI letting a *viewer* toggle between layers themselves, tabs-on-a-museum-placard style, with no Staff in the loop per-toggle — would be a real, different feature and *might* warrant a schema affordance, but that is explicitly **not** what kataribe wants (confirmed to `ferspas57-2` 2026-09-05): selection always happens in the Staff conversation.

**Also settled, same day — dossier content discipline**: a dossier must stay faithful to what its primary sources (GSI's document, kitavolca's actual checked tile data) directly establish — "don't awkwardly pad it" (hfu, 2026-09-05). Anything requiring a fresh/current lookup (present-day crop specifics, whether a memorial exists, this year's anniversary events) is deliberately left to narrative.json-generation time, when Staff can do real, citable research against a live source rather than freezing a guess into the versioned dossier — see `DOSSIER-FORMAT.md`'s "What belongs in a dossier vs. what gets looked up at generation time." Consequence for the first dossier: a claim like "the 1926 deposit area is farmland today" only belongs in the dossier's `facts` if it traces to the primary sources actually in hand (GSI's document as a 土地条件図 already classifies land use, or kitavolca's real VLCM data) — not because it was part of the original project-founding narrative in `HANDOVER.md`. Verify before asserting; mark `status: incomplete` rather than assume.

---

### D3 — Cartographer choice: `spiccato` first, `ferspas57` reserved for a future need this dossier doesn't have yet
**Date**: 2026-09-05
**What**: Settled (hfu) on `dwg7/spiccato` as the Cartographer kataribe's first dossier actually projects to, rather than forking `dwg7/ferspas57`'s narrative mechanism as originally planned in D1. `ferspas57` stays a live option for later, not ruled out.

**Why spiccato first**: (1) D2's follow-up already established kataribe's real shape is "one question → Staff resolves one layer → one resolved render," never a viewer-side toggle or an autoplayed multi-layer tour — which is structurally exactly `spiccato`'s single-Map-Intent-per-link model (ADR 0001/0004 baseline), not `ferspas57`'s sequential flyTo-tour model. (2) `spiccato` already has kitavolca's VLCM/VBM live and tested in its real catalog (`HANDOVER.md` D20, verified against `https://stars.optgeo.org/catalog` in a production-equivalent build) — `ferspas57`'s own catalog has no Hokkaido/volcano data at all, so forking it would additionally require new catalog-integration work this dossier doesn't need. (3) `staccato-spec` (session `staccato-spec-42`) independently recommended the same thing from the normative-architecture side: forking `ferspas57`'s narrative format buys no spec-level compatibility guarantee (ADR 0009 keeps it a single-implementation vocabulary), while `spiccato` gets ADR 0001/0004's baseline for free. (4) The first dossier's own `map_projection_hint` (hazard layer) is already a single camera+layer state, not a multi-step sequence — there was never an actual multi-step need to justify the heavier fork.

**Why `ferspas57` isn't ruled out**: if a *specific dossier layer* ever needs its own internal step-by-step build-up (e.g. "hazard, told as: flood-zone context → historic disaster extent → today's land use" as one guided sequence within a single layer — a different question from *which layer* to show), that is a real multi-step need `spiccato` doesn't support and `ferspas57`'s narrative mechanism does. hfu's call (2026-09-05): build on `spiccato` now, revisit `ferspas57` if/when a concrete dossier layer actually needs that.

**Consequence**: the dossier's `map_projection_hint` fields get projected into `spiccato` `#q=` links (see `dossiers/tokachidake-taisho-mudflow-1926.json`). No narrative.json is being built for the first dossier at this time.

---

### D4 — Converged with `ferspas57` on the Map Intent ↔ narrative.json ADR direction (via `staccato-ecosystem`/`staccato-spec`)
**Date**: 2026-09-07
**What**: `staccato-ecosystem` (coordinating with `staccato-spec-42`, ADR 0009's owner) proposed redefining `narrative.json` as a sequence of independent Map Intent documents (each optionally captioned) rather than an independently-shaped schema — Map Intent's own schema stays untouched, avoiding ADR 0009's Alternative-1 mistake (an optional `narrative` field grown onto Map Intent itself, causing step-vs-whole ambiguity). Asked kataribe and `ferspas57-2` to converge on three open points before an ADR gets drafted; both sides agreed on all three:

1. **`catalog_context`/`provenance` may duplicate across steps** (ADR 0007 Alternative 2's "reviewability over generality") rather than being hoisted to a wrapper level. `ferspas57-2`'s implementation note: with 373 existing real narratives (355 per-site + 15 tours + 3 curated) at their current lightweight per-step shape (`center`/`zoom`/`layers`/`caption` only), giving every step a full Map Intent needs real measurement of the LZString-compressed URL-length impact on longer (7–20 step) tours before implementation — not an ADR blocker, an implementation-phase item.
2. **`caption` is not redundant with Map Intent's `goal` field.** Verified against real content on both sides, independently reaching the same reasoning: `goal` and `caption` have fundamentally different speaker positions — `goal` describes what the *requester* wanted ("show Tokachidake"), `caption` speaks *to the viewer* while they're looking at the render. Kataribe's evidence: the first dossier's hazard-layer `map_caveat` field ("this polygon covers only the near-crater deposit, not the actual disaster's reach toward Kamifurano — don't let it imply otherwise") is a safety-relevant narrative statement, not a request description. `ferspas57`'s evidence: their DR Congo maize-siting narrative's "why did the constrained site score higher" mystery-caption is the same shape. Conclusion: `caption` stays a separate field; `goal` cannot absorb it.
3. **Sequence identity (`narrative_id`, step ordering) belongs at the wrapper level**, not fields inside each Map Intent step (no `step_index` etc. inside Map Intent itself).

**Why this matters for kataribe specifically**: makes D2's "a selected dossier layer is just an ordinary narrative with one step" conclusion even more literal — under this direction, a single-step case may not need a narrative wrapper at all, just a bare captioned Map Intent. Also validates `DOSSIER-FORMAT.md`'s `map_caveat` field design after the fact — it turned out to be exactly the content point 2's convergence needed, not a kataribe-specific idiosyncrasy.

**Status**: convergence reported to `staccato-ecosystem-85` 2026-09-07; ADR drafting is `staccato-spec-42`'s next step, not kataribe's — no action needed here until an ADR draft is circulated for review.

---

### D1 — Repo founded: a dossier-based narrative Staff, general-purpose, first instance is 十勝岳大正泥流
**Date**: 2026-09-05
**What**: Created `dwg7/kataribe` (public, CC0 1.0) at hfu's request, made during a `dwg7/staccato-ecosystem`-session Claude Code conversation, following an ecosystem-growth-strategy discussion that identified `dwg7/kitavolca` (Hokkaido volcano data pipeline) and `dwg7/kaga0` (air-gapped Cartographer appliance) as mature Library/Cartographer-side work with no matching Staff. A design proposal (drafted by a `model: fable` subagent, reviewed by hfu) settled three core design calls: (1) audience is "local intermediaries" only for v1 (disaster-prevention staff, teachers, tourism/onsen operators — not GSI-internal, not general public), framed as strengthening the accountable human chain; (2) a place/event's significance is multi-layered (hazard, livelihood, place-identity, ...) and Staff's job is recognizing which layer a specific asker needs, not covering all of them — to be validated here before being proposed as a `staccato-ecosystem` methodology principle, not before; (3) "lowering the bar" (pre-authored, versioned dossiers) is the primary constraint-workaround, even more heavily than `dwg7/ferspas57` used it — Staff selects and adapts register/language, never originates facts live.

**Naming**: `kataribe` (語り部, "one who carries a memory forward across generations, retelling it for whoever is listening") chosen over a volcano-specific name (hfu considered and rejected "volkataribe" — both because it reads awkwardly and because the repo is explicitly general-purpose; volcano is only the first domain, not the scope).

**First dossier**: 十勝岳大正泥流 — the 1926-05-24 Tokachidake mudflow disaster. Chosen partly because 2026 is its 100th anniversary, and partly because it's a near-perfect illustration of the layer-selection concept: the same land is simultaneously the 1926 hazard site and, today, farmland — disaster and livelihood are literally the same physical deposit at different points in time. Primary-source material (GSI's 1990 火山土地条件図「十勝岳」explanatory document) gathered the same day — see `HANDOVER.md`.

**Cartographer plan**: fork/adapt `dwg7/ferspas57`'s narrative-playback Cartographer mechanism (flyTo + layer-sync + caption panel, ADR-0004-compliant one-shot fragment handoff) rather than build one from scratch — but treat its actual JSON schema as something this repo has discretion to reshape, not a fixed contract to replicate (hfu's explicit call, given `ferspas57`'s own narrative format is itself still mid-revision). Verified the same day that `ferspas57`'s `main` branch still runs an older design (10-language pre-baked content) than a newer one (English-only, Staff does live adaptation) sitting unpushed in another session — coordinated directly with that session (`ferspas57-2`) rather than assuming either state.

**Why**: matches the established `dwg7` pattern — dedicated repo, dedicated session, `HANDOVER.md`/`CLAUDE.md` as onboarding (see `dwg7/staccato-ecosystem` D1, `dwg7/ferspas57` D1). hfu's own reasoning for placing this in the `dwg7` org from the start, rather than a personal-namespace prototype stage first (as `DWG7-CONTEXT.md`'s convention would otherwise suggest for early experiments): the underlying concept — 火山防災 grounded in official GSI/disaster-prevention-agency data — is already publicly and institutionally established, not a personal hobby project, so it doesn't need a "prove it's legitimate" prototype phase.
